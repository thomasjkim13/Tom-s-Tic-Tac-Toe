'use strict'
const config = require ('./../config')
const store = require ('./../store')

const signUp = function (dat) {
    return $.ajax ({
        method: 'POST'
    })
}

module.exports = {
    signUp
}